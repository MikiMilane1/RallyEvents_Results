from db import db
# from models import SSModel, ResultModel, EventModel


def sort_ss(event_model):
    event_id = event_model.id
    number_of_sections = event_model.ss_num
    # for ss in range(1, number_of_sections):
    #     ss_all = SSModel.query.join(ResultModel).join(EventModel).filter(EventModel.id == event_id, SSModel.ss_num == 1).or
    #     ss_sorted =
    return None
